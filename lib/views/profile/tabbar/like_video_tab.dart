import 'package:flutter/material.dart';

class LikedVideoTab extends StatelessWidget {
  const LikedVideoTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print("tab2");
    return Container(
      child: Text("Like Video"),
    );
  }
}
